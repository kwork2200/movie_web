import 'package:flutter/material.dart';

/// NavigatorObserver placeholder.
/// Back-navigation interstitial ads are handled directly in each detail screen's
/// _handleBack() method (MovieDetailsView / TVShowDetailsView) to avoid
/// double-triggering the ad counter.
class AdNavigatorObserver extends NavigatorObserver {}
